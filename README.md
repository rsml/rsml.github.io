# rsml.github.io

Run `make` after cloning if needed.

Local preview: open via a local web server (module scripts do not run on `file://`).
Example: `python3 -m http.server 8000` then visit `http://localhost:8000/`.
